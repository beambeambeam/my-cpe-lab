#include <math.h>
#include <stdio.h>


int main(int argc, char const *argv[]) {
  float a, b, c;
  float x1 = (-b + sqrt(b * b - 4 * a * c)) / (2 * a);
  float x2 = (-b + sqrt((b * b) - (4 * a * c))) / (2 * a);

  return 0;
}
